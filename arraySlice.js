// Array.prototype.slice()

const numbers = [1, 2, 3, 4, 5];

// slice methode will slice and return a part of an array. it takes starting index and the ending index to slice that part(it also doesn't change the main array(numbers))
const sliceResult = numbers.slice(0, 3);
console.log(sliceResult);
